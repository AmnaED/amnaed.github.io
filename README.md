Overview

This project runs a frontend website and a json-server backend using Docker and docker-compose.

Run the Project

docker compose up --build  - #run this command where this docker-compose.yml file is 

Stop the Project

docker compose down

